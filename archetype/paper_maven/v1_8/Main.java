package %s;

import org.bukkit.plugin.java.JavaPlugin;

public class %s extends JavaPlugin {

    @Override
    public void onEnable() {
        getLogger().info("%s enabled!");
    }

    @Override
    public void onDisable() {
        getLogger().info("%s disabled!");
    }
}
